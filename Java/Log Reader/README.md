### Log File Reader - Code Explanation

This program reads the `server.log` file line by line. It checks whether a line contains the word `"ERROR"`. If it does, that line is stored in an `ArrayList`. At the end, all error messages are displayed. This helps in finding important error logs quickly.
