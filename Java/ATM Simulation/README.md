# ATM Simulation

**Explanation:**  
This program demonstrates encapsulation by keeping the account balance private. The `withdraw()` method checks whether the withdrawal amount is valid before updating the balance.
